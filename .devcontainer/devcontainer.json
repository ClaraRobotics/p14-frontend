{
    "build": { "dockerfile": "Dockerfile" },
  
    "customizations": {
      "vscode": {
        "extensions": [
            "dbaeumer.vscode-eslint",
            "github.vscode-github-actions"
        ]
      }
    },
  
    // "forwardPorts": [],

    "mounts": [
      "type=bind,source=${localEnv:HOME}/.ssh,target=/mnt/.ssh,readonly"
    ]
  }
  