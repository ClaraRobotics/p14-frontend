import styled from 'styled-components';

const Page = styled.div`
  display: flex;
  flex-direction: column;
  height: calc(100% - 308px);
  overflow: hidden;
  padding: 0 20px;
`;

export default Page;
